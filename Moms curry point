import { useState } from "react"; import { motion } from "framer-motion"; import { Card, CardContent } from "@/components/ui/card"; import { Button } from "@/components/ui/button";

export default function MomsCurryPoint() { const [menu, setMenu] = useState([ { name: "Butter Chicken", price: "₹250", desc: "Creamy, rich and full of flavor." }, { name: "Paneer Tikka Masala", price: "₹220", desc: "Smoky paneer cooked in spicy tomato gravy." }, { name: "Mutton Rogan Josh", price: "₹300", desc: "Slow-cooked mutton in aromatic Kashmiri spices." }, { name: "Dal Tadka", price: "₹150", desc: "Classic yellow lentils tempered with ghee and spices." }, { name: "Chicken Biryani", price: "₹280", desc: "Fragrant basmati rice layered with spiced chicken." } ]);

return ( <div className="min-h-screen bg-amber-50 text-gray-800"> <header className="bg-amber-700 text-white py-6 shadow-md"> <div className="container mx-auto text-center"> <h1 className="text-4xl font-bold tracking-wide">Mom’s Curry Point</h1> <p className="text-lg mt-2">Homemade Taste, Served with Love ❤️</p> </div> </header>

<main className="container mx-auto p-6">
    <motion.h2
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.6 }}
      className="text-3xl font-semibold text-center mb-8"
    >
      Our Signature Dishes
    </motion.h2>

    <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {menu.map((item, index) => (
        <motion.div
          key={index}
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: index * 0.1 }}
        >
          <Card className="rounded-2xl shadow-lg hover:shadow-2xl transition-shadow">
            <CardContent className="p-6 text-center">
              <h3 className="text-2xl font-bold text-amber-800 mb-2">{item.name}</h3>
              <p className="text-gray-600 mb-3">{item.desc}</p>
              <p className="text-lg font-semibold text-amber-700">{item.price}</p>
            </CardContent>
          </Card>
        </motion.div>
      ))}
    </div>

    <div className="text-center mt-12">
      <Button className="bg-amber-700 hover:bg-amber-800 text-white text-lg px-8 py-4 rounded-full shadow-md">
        Order Now
      </Button>
    </div>
  </main>

  <footer className="bg-amber-800 text-white text-center py-4 mt-12">
    <p className="text-sm">© 2025 Mom’s Curry Point | Fresh. Authentic. Delicious.</p>
  </footer>
</div>

); }

